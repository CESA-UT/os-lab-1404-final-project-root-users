# Global configuration for root-monitor
# Define how processes should be sorted by default
# Supported values: cpu, mem
SORT_BY="cpu"

# Logging configuration
ENABLE_LOGGING=true
LOG_FILE="/var/log/root-monitor.log"